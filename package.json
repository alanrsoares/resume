{
  "name": "resume",
  "version": "1.0.0",
  "scripts": {
    "build": "resume export index.html --force",
    "serve": "http-server .",
    "start": "npm run build && npm run serve",
    "deploy": "npm run build && git add . && git commit -m 'update resume' && git push && open https://alanrsoares.github.io/resume/"
  },
  "homepage": "https://github.com/alanrsoares/resume",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "http-server": "^0.11.1",
    "resume-cli": "^1.1.2"
  }
}
